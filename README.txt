How to use this:

1. Make sure bin is in the root directory of your repo

2. Use:

chmod +x ./bin/quickpush

in the terminal to make the BASH file executable

3. Use:

./bin/quickpush

to run the script. Type a comment before you push.